api {
    username = test
    password = test
    username = ${?USERNAME}
    password = ${?PASSWORD}
    unauthenticatedUserId = 1
    unauthenticatedUsername = __SYSTEM__
    unauthenticatedUserId = ${?UNAUTHENTICATED_USER_ID}
    unauthenticatedUsername = ${?UNAUTHENTICATED_USERNAME}
}
